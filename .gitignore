.DS_Store
/AGENTS.md
/.build
/.build-tests
/dist
/docs/
/private/
/.swiftpm-home
/packaging/AppIcon-1024.png
/packaging/AppIcon.iconset/
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
.docs
.env
.env.*
*.pem
*.key
*.p12
*.cer
*.crt
.worktrace/

# ─── OS1 + Samantha runtime state (never commit) ───
# Live secrets and runtime data live under ~/.os1 on the user's machine.
# Anything brought into the repo for distribution is sanitized; nothing
# touching real keys should ever land in tracked files.
daemons/*/bot.pid
daemons/*/*.pid
daemons/*/bot.log
daemons/*/daemon.log
daemons/*/state.json
daemons/*/founder_chat_id
daemons/*/.env
launchd/*.local.plist
.os1-runtime/
*.credentials.json
